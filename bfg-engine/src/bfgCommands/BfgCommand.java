package bfgCommands;

/**
 * This class abstracts any executable command in the form of a request (eg 
 * request from player to discard item) or an immediate effect to take place (eg move marker on Track).
 * @author kostadinos
 */
public abstract class BfgCommand implements Command {
	
}
